{
  "name": "estimator-rpa",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
