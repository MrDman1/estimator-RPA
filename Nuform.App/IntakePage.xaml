<Page x:Class="Nuform.App.IntakePage"
      xmlns:local="clr-namespace:Nuform.App"
      xmlns:core="clr-namespace:Nuform.Core.Domain;assembly=Nuform.Core"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Intake">
    <StackPanel Margin="10" VerticalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Estimate #" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="EstimateNumberBox" Width="100"/>
            <TextBlock Text="Cont %" Margin="20 0 0 0" VerticalAlignment="Center"/>
            <TextBox x:Name="ContBox" Width="50" Text="5"/>
        </StackPanel>
        <TextBlock Text="Rooms" FontWeight="Bold"/>
        <DataGrid x:Name="RoomsGrid" AutoGenerateColumns="False" ColumnWidth="Auto" MinColumnWidth="100" RowHeaderWidth="0" GridLinesVisibility="All" Height="120" Margin="0 0 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="L" Binding="{Binding LengthFt}" />
                <DataGridTextColumn Header="W" Binding="{Binding WidthFt}" />
                <DataGridTextColumn Header="H" Binding="{Binding HeightFt}" />
                <DataGridTextColumn Header="WallPanelLen" Binding="{Binding WallPanelLengthFt}" />
                <DataGridComboBoxColumn Header="PanelWidth" SelectedItemBinding="{Binding PanelWidthInches}" ItemsSource="{x:Static local:IntakePage.PanelWidths}" />
                <DataGridComboBoxColumn Header="WallColor" SelectedItemBinding="{Binding WallPanelColor}" ItemsSource="{x:Static local:IntakePage.PanelColors}" />
                <DataGridCheckBoxColumn Header="Ceiling" Binding="{Binding HasCeiling}" />
                <DataGridTextColumn Header="CeilingLen" Binding="{Binding CeilingPanelLengthFt}" />
                <!-- Bind the ceiling orientation in TwoWay mode and commit changes immediately. -->
                <DataGridComboBoxColumn Header="Orientation"
                                        SelectedItemBinding="{Binding CeilingOrientation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{x:Static local:IntakePage.CeilingOrientations}" />
                <DataGridComboBoxColumn Header="CeilColor" SelectedItemBinding="{Binding CeilingPanelColor}" ItemsSource="{x:Static local:IntakePage.PanelColors}" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Text="Openings" FontWeight="Bold"/>
        <DataGrid x:Name="OpeningsGrid" AutoGenerateColumns="False" Height="120" Margin="0 0 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Width" Binding="{Binding Width}" />
                <DataGridTextColumn Header="Height" Binding="{Binding Height}" />
                <DataGridTextColumn Header="Count" Binding="{Binding Count}" />
                <DataGridComboBoxColumn Header="Treatment"
                                        SelectedItemBinding="{Binding Treatment}"
                                        ItemsSource="{x:Static local:IntakePage.OpeningTreatments}" />
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Header="Trim Options" Margin="0 0 0 10">
            <StackPanel x:Name="TrimPanel" Margin="5">
                <CheckBox x:Name="JTrimCheckBox" Content="Use J-Trim" IsChecked="True"/>
                <StackPanel x:Name="CeilingTransitionPanel" Orientation="Horizontal" Margin="0 5 0 0">
                    <TextBlock Text="Ceiling Transition:" VerticalAlignment="Center" Margin="0 0 5 0"/>
                    <RadioButton GroupName="CeilingTransition" Content="None" Tag="" IsChecked="True" Margin="0 0 5 0"/>
                    <RadioButton GroupName="CeilingTransition" Content="Crown/Base" Tag="crown-base" Margin="0 0 5 0"/>
                    <RadioButton GroupName="CeilingTransition" Content="Cove" Tag="cove" Margin="0 0 5 0"/>
                    <RadioButton GroupName="CeilingTransition" Content="F" Tag="f-trim"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Hardware &amp; Accessories" Margin="0 0 0 10">
            <StackPanel>
                <CheckBox Content="Include Wall Screws (Concrete)" IsChecked="{Binding Input.IncludeWallScrews, Mode=TwoWay}" />
                <CheckBox Content="Include Ceiling Screws (Stainless)" IsChecked="{Binding Input.IncludeCeilingScrews, Mode=TwoWay}" />
                <CheckBox Content="Include Plugs" IsChecked="{Binding Input.IncludePlugs, Mode=TwoWay}" />
                <CheckBox Content="Include Spacers" IsChecked="{Binding Input.IncludeSpacers, Mode=TwoWay}" />
                <CheckBox Content="Include Expansion Tool" IsChecked="{Binding Input.IncludeExpansionTool, Mode=TwoWay}" />
            </StackPanel>
        </GroupBox>

        <Button Content="Next" HorizontalAlignment="Right" Width="80" Click="Next_Click"/>
    </StackPanel>
</Page>
